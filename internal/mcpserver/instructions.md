# Guided Study

Use stable `book_id` and `session_id` values on scoped calls.

List sessions before resuming study.

Page indices are 1-based physical PDF pages.

Use expected values exactly for:

- cursors
- log tails
- deck revisions
- card revisions

Resolve conflicts before retrying.
