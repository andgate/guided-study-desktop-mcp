interface:
  display_name: "Noggin"
  short_description: "Study books actively or create flashcard decks"
  default_prompt: "Use $noggin to continue my active study session."

policy:
  allow_implicit_invocation: true
